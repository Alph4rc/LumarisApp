# 项目结构说明

## 1. 项目概述

这是一个跨平台的应用程序，主要用于提供教育相关的功能，包括课程表、成绩查询、校车查询等功能。

## 2. 目录结构

### 2.1 根目录结构

```
├── android/           # Android平台相关代码
├── assets/            # 静态资源文件
├── coverage/          # 测试覆盖率报告
├── ios/               # iOS平台相关代码
├── lib/               # 主应用代码
├── linux/             # Linux平台相关代码
├── macos/             # macOS平台相关代码
├── test/              # 测试代码
├── web/               # Web平台相关代码
├── windows/           # Windows平台相关代码
├── .gitignore         # Git忽略文件
├── LICENSE            # 许可证文件
├── README.md          # 项目说明文档
├── analysis_options.yaml # 代码分析配置
├── devtools_options.yaml # DevTools配置
├── pubspec.lock       # 依赖锁定文件
├── pubspec.yaml       # 依赖配置文件
└── PROJECT_STRUCTURE.md # 项目结构说明文档
```

### 2.2 lib目录结构

```
├── core/              # 核心功能模块
│   ├── models/        # 数据模型
│   ├── services/      # 核心服务
│   └── utils/         # 工具函数
├── features/          # 功能模块
│   ├── club/          # 社团功能
│   │   ├── models/    # 社团数据模型
│   │   └── services/  # 社团服务
│   ├── education/     # 教育功能
│   │   ├── controllers/ # 教育功能控制器
│   │   └── services/  # 教育服务
│   └── system/        # 系统功能
│       ├── notifications/ # 通知服务
│       └── update/    # 更新服务
├── platform/          # 平台特定代码
│   ├── android/       # Android特定代码
│   ├── ios/           # iOS特定代码
│   └── macos/         # macOS特定代码
├── routes/            # 路由配置
├── state/             # 状态管理
├── ui/                # UI相关代码
│   ├── components/    # UI组件
│   ├── layouts/       # 布局组件
│   └── pages/         # 页面组件
├── bottom_navigation.dart # 底部导航
├── main.dart          # 应用入口
├── main_app.dart      # 主应用组件
├── modern_sidebar.dart # 现代化侧边栏
└── under_maintenance_screen.dart # 维护中屏幕
```

## 3. 目录功能说明

### 3.1 core目录

core目录包含应用的核心功能模块，这些功能模块不依赖于特定的业务逻辑，可以在整个应用中复用。

#### 3.1.1 core/models

包含应用中使用的所有数据模型，如课程模型、成绩模型、用户模型等。

#### 3.1.2 core/services

包含应用的核心服务，如网络服务、数据存储服务、时间服务等。

#### 3.1.3 core/utils

包含应用中使用的工具函数，如图片加载工具、性能监控工具、请求缓存工具等。

### 3.2 features目录

features目录包含应用的各个功能模块，每个功能模块都是相对独立的，可以单独开发和测试。

#### 3.2.1 features/club

包含社团相关的功能，如社团成员管理、项目管理、任务管理等。

#### 3.2.2 features/education

包含教育相关的功能，如课程表、成绩查询、校车查询等。

#### 3.2.3 features/system

包含系统相关的功能，如通知服务、更新服务、设置管理等。

### 3.3 platform目录

platform目录包含平台特定的代码，这些代码只在特定的平台上运行。

#### 3.3.1 platform/android

包含Android平台特定的代码，如后台服务、下载服务等。

#### 3.3.2 platform/ios

包含iOS平台特定的代码，如后台服务等。

#### 3.3.3 platform/macos

包含macOS平台特定的代码，如macOS UI侧边栏等。

### 3.4 routes目录

包含应用的路由配置，定义了应用中各个页面之间的导航关系。

### 3.5 state目录

包含应用的状态管理代码，使用GetX框架进行状态管理。

### 3.6 ui目录

包含应用的UI相关代码，如页面组件、UI组件、布局组件等。

#### 3.6.1 ui/components

包含应用中使用的UI组件，如卡片组件、模态框组件、空状态组件等。

#### 3.6.2 ui/layouts

包含应用中使用的布局组件，如侧边栏布局、底部导航布局等。

#### 3.6.3 ui/pages

包含应用中的所有页面，如首页、课程表页面、成绩查询页面等。

## 4. 代码组织原则

1. **按功能模块组织代码**：将相关功能的代码组织在一起，便于开发和维护。
2. **高内聚低耦合**：每个模块内部的代码应该高度内聚，模块之间的耦合应该尽可能低。
3. **代码复用**：尽量复用已有的代码，避免重复开发。
4. **平台无关性**：核心功能代码应该平台无关，平台特定的代码应该放在platform目录下。
5. **易于测试**：代码应该易于测试，便于编写单元测试和集成测试。

## 5. 导入路径规范

1. **相对导入**：同一模块内部的文件使用相对导入。
2. **绝对导入**：不同模块之间的文件使用绝对导入，格式为`package:ios_club_app/模块名/子模块名/文件名.dart`。
3. **避免循环导入**：避免模块之间的循环导入，保持导入关系的清晰。

## 6. 开发流程

1. **需求分析**：分析需求，确定功能模块。
2. **设计**：设计数据模型、API接口、UI组件等。
3. **开发**：按照目录结构组织代码，编写功能代码。
4. **测试**：编写单元测试和集成测试，确保代码质量。
5. **部署**：构建应用，部署到各个平台。

## 7. 代码风格

1. **命名规范**：使用驼峰命名法，类名首字母大写，方法名和变量名首字母小写。
2. **代码格式**：使用dartfmt格式化代码，保持代码风格的一致性。
3. **注释**：关键代码添加注释，解释代码的功能和实现思路。
4. **文档**：编写API文档，便于其他开发者使用。

## 8. 注意事项

1. **平台兼容性**：开发时需要考虑不同平台的兼容性，避免使用平台特定的API。
2. **性能优化**：关注应用的性能，避免不必要的计算和渲染。
3. **安全性**：保护用户数据的安全，避免敏感数据泄露。
4. **可维护性**：编写易于维护的代码，便于后续的功能扩展和bug修复。

## 9. 总结

本项目采用了模块化的设计思路，将应用划分为多个功能模块，每个模块内部的代码高度内聚，模块之间的耦合度低。这种设计方式便于开发和维护，同时也便于后续的功能扩展。

通过遵循上述的代码组织原则和导入路径规范，可以提高代码的可维护性、可读性和开发效率，使新功能开发和后续维护更加便捷。